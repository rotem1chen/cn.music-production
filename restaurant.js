/* ============================================================
   RESTAURANT — food & venue work.
   Shown on restaurant.html, reached from the RESTAURANT tile.
   Each { ... } is one video. To add one, copy a line.

   url:     YouTube / youtu.be / Shorts / .mp4 link  (required)
   title:   shown under the tile and in the player
   artist:  the place or client. Optional.
   thumb:   cover image — optional for YouTube (auto), needed for .mp4
   ratio:   optional per-video shape, e.g. "9 / 16" for a vertical one
   ============================================================ */

const RESTAURANT = [
  // { url: "https://youtu.be/XXXXXXXXXXX", title: "…", artist: "…" },
];
